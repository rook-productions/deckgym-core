//! Bot behaviour tests. Positions are hand-built from card text in `database.json` and each test
//! names the decision the position is about.
#[path = "bots/opponent_aware_test.rs"]
mod opponent_aware_test;
